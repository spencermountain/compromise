// Generated by CoffeeScript 1.6.3
var all, arr, lexicon, rules;

require("/Users/spencer/mountain/dirty");

lexicon = require("./lexicon").lexicon;

rules = require("./new_rules").rules;

arr = [];

Object.keys(rules).each(function(k) {
  return rules[k].each(function(r) {
    return arr.push({
      reg: r,
      pos: k
    });
  });
});

rules = arr;

all = Object.keys(lexicon).spigot(function(k) {
  return rules.some(function(r) {
    if (k.match(r.reg)) {
      if (lexicon[k].toLowerCase().substr(0, 2) !== r.pos.toLowerCase()) {
        return console.log([k, lexicon[k], r.pos].join("\t"));
      }
    }
  });
});
