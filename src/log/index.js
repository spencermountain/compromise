const chalk = require('chalk');

module.exports = {
  enable: () => {
  },
  here: () => {
  },
  show: () => {
  },
  tag: (t, pos, reason, path) => {
    // console.log('       ' + chalk.green(t.normal) + ' -> ' + chalk.red(pos) + '    (' + reason + ')');
  },
  change: () => {
  }
};
