'use strict';
//grab the terms related to this term
const pluck = {
  target: (t) => {
    // let before = t.before(3);
    let after = t.after(3);
    // console.log(after);
    // let len = before.length;
    return null;
  }
};
module.exports = pluck;
