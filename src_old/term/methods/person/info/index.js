'use strict';
const parseName = require('./parseName');
const gender = require('./gender');

const info = {
  parsedName: (t) => {
    return parsedName(t)
  },
  gender: (t) => {
    return parsedName(t)
  }
}
module.exports = info
